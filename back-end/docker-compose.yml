services:
  db:
    image: postgres:15
    container_name: petshop_postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: petshop_db
    ports:
      - "5432:5432"